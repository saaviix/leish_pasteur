# Dependances du projet leishmaniose / P. sergenti (Maroc)
# Installation :  pip install -r requirements.txt

# --- traitement de donnees ---
pandas>=2.0
numpy>=1.24
scipy>=1.10
networkx>=3.0

# --- climat (NetCDF ERA5) ---
xarray>=2023.1
netcdf4>=1.6
cdsapi>=0.7          # telechargement ERA5 depuis le Climate Data Store (CDS)

# --- modele bayesien (occupancy ICAR/BYM2) ---
pymc>=5.10
pytensor>=2.18
arviz>=0.17
# linker numba requis par pytensor sur machines sans compilateur C (cf.
# PYTENSOR_FLAGS="cxx=" dans bayesian_occupancy.py/occupancy_gp.py) --
# plafonne car les versions plus recentes cassent la compat pytensor 3.x
numba>=0.58,<=0.65.1

# --- modeles de prediction (GBM spatio-temporel, PINN, ensemble, comparatif) ---
lightgbm>=4.0
scikit-learn>=1.3
torch>=2.0
joblib>=1.3
statsmodels>=0.14   # model_benchmark.py : GLM/NB
pygam>=0.9           # model_benchmark.py : GAM splines
openpyxl>=3.1         # lecture mun_pop_source.xlsx (fix_mun_pop.py)

# --- scraping / extraction ---
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9
pdfplumber>=0.10

# --- graphiques / analyse ---
matplotlib>=3.7
seaborn>=0.13

# --- tableau de bord ---
flask>=3.0
